const OP = require('../shared/opcodes');

module.exports = function(image) {
    let stack = [];
    var sp = -1;
    var cp = 0;

    function cpu(code) {

    }

    function string(address) {


    }
}
